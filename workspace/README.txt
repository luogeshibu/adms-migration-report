Runtime workspace root.

Create one subfolder per site, for example:
  ADF/
  ABN2/

Site source files, generated reports, snapshots and project.db are runtime data and should not be committed to source control.
